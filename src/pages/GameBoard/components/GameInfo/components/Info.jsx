import React from "react"
import styled from "styled-components"

const Wrapper = styled.div``

const Info = ({ turns }) => {
  return (
    <Wrapper>
      
    </Wrapper>
  )
}

export default Info
